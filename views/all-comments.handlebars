<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>All Comments</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.1/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-+0n0xVW2eSR5OomGNYDnhzAbDsOXxcvSN1TPprVMTNDbiYZCxYbOOl7+AMvyTG2x" crossorigin="anonymous">
  <link rel="stylesheet" href="/css/style.css">
  <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:opsz,wght,FILL,GRAD@24,400,0..1,0">
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Work+Sans:wght@400;500&display=swap" rel="stylesheet">
</head>
<body>
  {{> default-navbar }}

  <section class="section all-comments">
    <div class="container">
      {{#if comments.length}}
        <h3 class="page-title title-large">All Comments</h3>
        <div class="comment-list">
          {{#each comments as |comment|}}
          <div class="comment-item border-b border-gray-200 py-4">
            <p><strong>{{comment.user.username}}:</strong> {{comment.comment_text}}</p>
            <p><em>Posted on {{format_date comment.createdAt}}</em></p>
            {{#if comment.blog}}
            <div class="comment-post mt-2">
              <h4>Comment on Blog: <a href="/blog/{{comment.blog.id}}" class="text-blue-500 hover:underline">{{comment.blog.title}}</a></h4>
              <p>{{comment.blog.description}}</p>
            </div>
            {{/if}}
            {{#if comment.article}}
            <div class="comment-post mt-2">
              <h4>Comment on Article: <a href="/article/{{comment.article.id}}" class="text-blue-500 hover:underline">{{comment.article.title}}</a></h4>
              <p>{{comment.article.description}}</p>
            </div>
            {{/if}}
          </div>
          {{/each}}
        </div>
      {{else}}
        <p class="text-center">There are no comments yet!</p>
      {{/if}}
    </div>
  </section>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.1/dist/js/bootstrap.bundle.min.js" integrity="sha384-gtEjrD/SeCtmISkJkNUaaKMoLD0//ElJ19smozuHV6z3Iehds+3Ulb9Bn9Plx0x4" crossorigin="anonymous"></script>
</body>
</html>
